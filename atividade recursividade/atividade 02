function somaRecursivaFracao(N) {
    
    if (N === 1) {
        return 1;
    }
   
    return 1/N + somaRecursivaFracao(N - 1);
}

const numero = 5;
const resultado = somaRecursivaFracao(numero);
console.log("A soma de 1 + 1/2 + 1/3 + ... + 1/" + numero + " é:", resultado);
